name: document_scanner_example
description: Example app for the Document Scanner package - Android focused.
version: 3.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.8.1
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

  # Local package dependency
  document_scanner:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
